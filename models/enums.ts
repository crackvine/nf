export enum Fellowship {
  fellows = "fellows",
  angels = "angels",
  writers = "writers"
}

export enum FeedEventType {
  new_user = "new_user",
  new_project = "new_project",
  announcement = "announcement"
}
